<!DOCTYPE html>
<html lang="en">
<head profile="http://gmpg.org/xfn/11">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= h(page_title) %></title>
  <%= stylesheet_link_tag "/stylesheets/theme/bootstrap.css", "/stylesheets/theme/style.css", "user-styles.css", :media => 'all' %>
  <%= render 'shared/page_header' %>
</head>
<body>
  <nav class="navbar navbar-default" role="navigation">
    <div class="container">
      <div class='navbar-header'>
        <%= link_to(content_tag(:span, '', class: 'glyphicon glyphicon-th'), this_blog.base_url, class: 'navbar-brand' )%>
        <ul class="nav navbar-nav navbar-left">
        <% item_type_gets.each do |item| %>
                <% if item.submenu.count == 0 %>
                    <li><%= link_to item.name, item_path(item) %></li>
                <% else %>
                    <li class="dropdown">
                        <a href="<%= item_path(item.submenu.first) %>" class="dropdown-toggle" data-toggle="dropdown" ><%= item.name %>
                            <b class="caret"></b>
                        </a>
                        <!--<button class="btn btn-default dropdown-toggle" type="button" id="toggle_menu1" date-toggle="dropdown"  aria-haspopup="true" aria-expanded="true">
                            <%= item.name %>
                            <span class="caret"></span>
                        </button>-->
                        <ul class="dropdown-menu">
                            <% item.submenu.each do |submenu| %>
                            <li><%= link_to  submenu.name, item_path(submenu) %></li>
                            <% end %>
                        </ul>
                    </li>
                <% end %>
        <% end %>
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= "test_menu" %>
                    <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li><a href="#">主页</a></li>
                    <li role="separator" class="divider"></li>
                    <li><a href="#">关于我</a></li>
                    <li><a href="#">通知</a></li>
                    <li role="separator" class="divider"></li>
                    <li><%= link_to "登出" , "#", method: 'delete' %></li>
                    <!--
				  <li><a href="#">Something else here</a></li>
				  <li role="separator" class="divider"></li>
				  <li><a href="#">Separated link</a></li>
				  -->
                </ul>
            </li>
        </ul>
      </div>
      <ul class='nav navbar-nav pull-right'>
        <!--<li><%= link_to(t('.statuses'), {controller: 'notes'}) %></li>-->
        <!--<li><%= link_to(t('.archives'), {controller: 'articles', action: 'archives'}) %></li>-->
        <%- Page.published.each do |page| %>
        <li><%= link_to_permalink(page, page.title) %></li>
        <%- end %>
      </ul>
    </div>
  </nav>

  <div id='logo' class='container'>
    <div class='row'>
      <div class='col-lg-12 col-sm-12 vertical-container'>
        <div class='vertical'>
          <h1><%= link_to(this_blog.blog_name, this_blog.base_url) %></h1>
          <p class='subtitle'><%= this_blog.blog_subtitle %></p>
        </div>
      </div>
    </div>
  </div>
  <div class='container' id='page'>
    <div class='col-sm-8'>
      <%= yield %>
    </div>
    <div class='col-sm-3 col-sm-offset-1' id='sidebar'>
      <%= render_sidebars %>
    </div>
  </div>

  <div id='page' class='container'>
    <div class='row'>
      <div class='span1'></div>
      <div class='span7'></div>
    </div>
  </div>
  <!--<p style='text-align: center'><small>Powered by <a href='http://publify.co'>Publify</a> | Photo <a href='http://startupstockphotos.com/'>Startup stock photos</a></small></p>-->
</body>
</html>
